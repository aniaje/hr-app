import { Button } from "../components/AvatarMenu/Button";

const Errorpage = () => {
  return (
    <main>
      <h3>Page not Found</h3>
      <Button buttonText="Go to Start" />
    </main>
  );
};

export default Errorpage;

// onClick={() => navigate("/")}
